[package]
name = "inspect-table"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
arrow-array = { version = "^46.0" }
arrow-schema = { version = "^46.0" }
deltakernel = { path = "../../../kernel" , features = ["default", "tokio", "developer-visibility"]}
clap = { version="^4.4", features = ["derive"] }
url = "2"
